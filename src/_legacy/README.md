These files are the earlier modular bring-up scaffold.

They are intentionally not part of any current CMake target.

They remain here as historical reference while the shipped firmware lives in `src/claude_buddy_pico.cpp`.
