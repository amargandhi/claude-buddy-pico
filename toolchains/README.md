# Local Toolchains

This directory is for workspace-local toolchains that do not need a system-wide install.

Use `scripts/extract_local_toolchain.sh` to unpack the cached Arm GNU embedded package here when the Homebrew cask install is not practical.
