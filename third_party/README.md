# Third-Party Dependencies

This directory is where `scripts/clone_deps.sh` places vendor repositories needed for local builds:

- `pico-sdk`
- `pico-examples`
- `pimoroni-pico`
- `pimoroni-pico-rp2350`

These trees are intentionally ignored by Git in this repo scaffold.
